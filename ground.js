class Ground{
    constructor(X,Y,width,height){
        var options = {
            isStatic : true,
            restitution : 0,
            friction : 0,
            density : 1
        }
    }
    thisbody = Bodies.rectangle(X,Y,width,height,options);
    thiswidth = width;
    thisheight = height;

    Worldadd(world,thisbody);
    }
Display()
rectMode(CENTER);
fill(255);
var pos = this.body.position;
rect[this.body.position.X , this.bpdy.position.Y , this.width , this.height];
